if SHIELD_KRYLO_LEFT
    config ZMK_KEYBOARD_NAME
        default "krylo"
    config ZMK_SPLIT_ROLE_CENTRAL
        default y
endif

if SHIELD_KRYLO_LEFT || SHIELD_KRYLO_RIGHT
    config ZMK_BLE # Needs to be explicit
        default y
    config ZMK_SPLIT
        default y
endif
